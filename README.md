# PolicyNotesServiceApi
Building a small microservice PolicyNotesServiceApi for an insurance company to store and retrieve internal notes for customer insurance policies (id, policyNumber, Note).  This microservice is implemented using: .NET 8 Minimal APIs, EF Core InMemory Database, Repository + Service layers, Unit tests &amp; Integration tests using xUnit. 
